#include <iostream>
using namespace std;

int main() {
    const int MAX_USERS = 100; // here const is used so it will not allow the compiler to alter it keeps same value till the end of program.

    cout << "Max Users: " << MAX_USERS << endl;// simply constant is function is used to maintain same thing no changes.

    return 0;
}